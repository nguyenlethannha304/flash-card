import { RenderErrorsPipe } from './render-errors.pipe';

describe('RenderErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
